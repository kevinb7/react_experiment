{
  "name": "react_experiment",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "jsx --extension jsx --target es5 --harmony src/ build/",
    "watch": "jsx --extension jsx --target es5 --harmony --watch src/ build/"
  },
  "author": "Kevin Barabash (kevinb7@gmail.com)",
  "license": "MIT",
  "dependencies": {
    "react": "^0.13.1"
  }
}
